apiVersion: v1
kind: Service
metadata:
  name: featureflag
  labels:
    app: featureflag
    service: featureflag
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      name: http
  selector:
    service: featureflag
  