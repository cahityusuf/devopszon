apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations: {}
  name: devopszon-httpapi-host-ingress
  namespace: devopszon
spec:
  ingressClassName: nginx
  rules:
  - host: api.devopszon.com
    http:
      paths:
      - backend:
          service:
            name: devopszon-httpapi-host
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - api.devopszon.com
    secretName: devopszon-httpapi-host-ingress-secret