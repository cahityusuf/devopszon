apiVersion: v1
kind: Service
metadata:
  name: shopping-client
  labels:
    app: shopping-client
    service: shopping-client
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      name: http
  selector:
    service: shopping-client
  